# AI 游戏 NPC 记忆系统 — 功能清单与开发计划 v5

> 定位：AI 驱动游戏流程的子模块，解决 NPC 长期记忆问题。  
> 当前形态：**四子 Agent 流水线**封装为单 NPC Agent，聊天机器人原型验证。  
> 领域：**娱乐/聊天/游戏**，非工作场景。

---

## 一、核心架构：A1-A4 流水线 → 封装为单 NPC Agent

```
                          ┌──────────────────────────────────────────┐
                          │          单 NPC Agent（大Agent）          │
                          │                                          │
   用户输入 ─────────────▶ │  ┌────────────┐        ┌───────────┐    │
                          │  │ A1 记忆搜索 │        │ A4 记忆管理│    │
                          │  │ (必须)     │◀──────▶│ (并行监听) │    │
                          │  └─────┬──────┘        └─────┬─────┘    │
                          │        │ 候选记忆            │事后整理   │
                          │        ▼                     │           │
                          │  ┌──────────────────────┐    │           │
                          │  │ A2 深度思考(含分类)   │    │           │
                          │  │ (必须)               │    │           │
                          │  │                      │    │           │
                          │  │ 主任务: 深度思考       │    │           │
                          │  │ ①相关性初筛           │    │           │
                          │  │ ②冲突检测             │    │           │
                          │  │ ③证据空白检测         │    │           │
                          │  │ ④最终筛选             │    │           │
                          │  │                      │    │           │
                          │  │ 副任务: 任务分类       │    │           │
                          │  │ → 闲聊模式(默认)      │    │           │
                          │  │ → 知识模式(按需)      │    │           │
                          │  │                      │    │           │
                          │  │ 自主决策:             │    │           │
                          │  │ 根据A1候选记忆数据     │    │           │
                          │  │ +用户问题复杂度，     │    │           │
                          │  │ 决定是否启用深度思考   │    │           │
                          │  └──────────┬───────────┘    │           │
                          │             │ 模式+筛选记忆   │           │
                          │             ▼                  │           │
                          │  ┌────────────┐              │           │
                          │  │ A3 最终输出 │◀─────────────┘           │
                          │  │ (必须)     │  A4输出:                 │
                          │  └─────┬──────┘  摘要/事件/分类           │
                          └────────┼─────────────────────────────────┘
                                   │
                                   ▼
                              用户看到回复
```

**A1-A4 职责分工：**

| Agent | 名称 | 调用 | 主任务 | 副任务 |
|-------|------|:--:|--------|--------|
| **A1** | 记忆搜索 | 必须 | 多路检索（关键词+语义），输出候选记忆+摘要 | — |
| **A2** | 深度思考 | 必须 | 四步推理筛选记忆（初筛→冲突→空白→筛选） | 任务分类（闲聊/知识），控制记忆权重 |
| **A3** | 最终输出 | 必须 | 结合筛选记忆+人设+模式，生成NPC回复 | — |
| **A4** | 记忆管理 | 并行+事后 | 摘要整理、事件分割、分类归档、记忆升降级 | — |

**A2 自主决策深度思考启用：**
- A2 每次都启动，但**自己决定是否走深度思考四步推理**
- 决策依据：A1 给的候选记忆数量、相关性分数、用户问题复杂度
- 简单闲聊 → 跳过深度思考，只做分类，直接放行
- 简单知识问答 → 可能也跳过深度思考（答案明确，无需推理）
- 复杂问题/记忆冲突 → 启用完整四步推理

**A2 双模式：**

| 模式 | 触发 | 深度思考 | 记忆检索权重 | 记忆写入权重 | 回答风格 |
|------|------|:--:|:--:|:--:|------|
| **闲聊模式** | 默认 | 通常不启用 | 正常 | 正常 | 自然随意 |
| **知识模式** | 用户知识问题 | 复杂时启用 | 降低 | 降低（仍写入） | 严谨+人设 |

**A4（记忆管理）的工作模式：**
- **并行阶段**：A1-A3 工作时，A4 在后台监听本轮对话，准备摘要草稿
- **事后阶段**：A3 输出回复后，A4 正式执行：
  - ① 对本轮对话生成摘要
  - ② 判断是否触发事件分割（参考 ES-Mem 的 Event Segmentation Theory）
  - ③ 决定存储层级（核心/情景/工作记忆）
  - ④ 执行记忆升降级（如重要事件从工作记忆升级到情景记忆）

---

## 二、记忆三层模型（娱乐/游戏场景适配）

| 层级 | 名称 | 游戏场景对应 | 半衰期 | 注入方式 | 检索权重 |
|------|------|-------------|--------|----------|:--:|
| **L1** | **核心记忆 (Core)** | NPC身份、性格、世界观、与玩家的关系基调 | 0天（永不衰减） | 每轮强制注入 | 固定 |
| **L2** | **情景记忆 (Episodic)** | 关键剧情事件、重要对话、关系变化、任务进展 | 60天 | 检索召回 | 高 |
| **L3** | **工作记忆 (Working)** | 近期闲聊、当前对话上下文、临时信息 | 7天 | 按需检索 | 低 |

> **衰减公式**（借鉴 wanyimem）：`recency_factor = 2^(-days_since_access / halflife)`  
> - 钉住记忆（pinned=true）：halflife = 0，永不衰减  
> - 权重<0.15 视为过时（≈2.7个半衰期），Agent4 可触发降级

**与工作场景的区别：**
- 核心记忆更侧重"角色扮演"（性格、说话风格、情感倾向）而非"规则约束"
- 情景记忆更侧重"剧情叙事"（事件链、情感弧线）而非"任务追踪"
- 工作记忆更侧重"对话流畅度"（话题连续性）而非"执行状态"

---

## 三、存储方案：JSON 多文件

| 特性 | 说明 |
|------|------|
| 存储格式 | 每个记忆条目 = 一个 JSON 文件 |
| 目录结构 | `data/memory/core/` `episodic/` `working/` |
| 文件命名 | `{timestamp}_{memory_id}.json` |
| 索引加速 | `index.json` 包含所有记忆元数据（id、摘要、关键词、衰减权重） |
| 事件溯源 | Append-only，旧文件不删除 |

**每个记忆 JSON 文件结构：**
```json
{
  "memory_id": "mem_20260824_001",
  "layer": "episodic",
  "content": "原始完整内容...",
  "summary": "LLM生成的摘要（压缩后，检索阶段只看摘要，减少token）",
  "keywords": ["森林", "龙", "战斗"],
  "timestamp": "2026-08-24T10:30:00",
  "last_accessed": "2026-08-24T15:00:00",
  "access_count": 3,
  "importance": 0.7,
  "decay_weight": 0.85,
  "pinned": false,
  "emotional_tag": "excited",
  "event_id": "event_dragon_encounter",
  "tags": ["剧情", "战斗", "龙"]
}
```

---

## 四、功能清单

### 阶段 1：基础骨架

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| 1.1 | **AI 模型调用封装** | 借鉴 `ai_server.py` 的 `call_ai_api()`，OpenAI 兼容接口，config.txt 配置 | P0 |
| 1.2 | **JSON 文件存储引擎** | 记忆 CRUD，索引管理，三层目录结构 | P0 |
| 1.3 | **NPC 人设配置** | `character_config.json` 定义 NPC 身份、性格、世界观（存入核心记忆） | P0 |
| 1.4 | **Token 统计** | 每次 LLM 调用记录 token 消耗 | P1 |

### 阶段 2：四 Agent 流水线核心

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| 2.1 | **A1：记忆搜索** | 多路检索（关键词 jieba + 倒排索引 + 可选向量），输出候选记忆含摘要 | P0 |
| 2.2 | **A2：深度思考** | 主任务：四步推理筛选记忆；副任务：分类闲聊/知识模式，控制记忆权重；自主决策是否启用深度思考 | P0 |
| 2.3 | **A3：最终输出** | 结合筛选记忆 + 核心记忆(人设) + 当前对话，生成 NPC 风格回复 | P0 |
| 2.4 | **A4：记忆管理** | 摘要整理 + 事件分割(ES-Mem) + 分类归档 + 记忆升降级 | P0 |
| 2.5 | **流水线编排器** | 协调四个 Agent 的调用顺序、数据传递、并行/串行切换 | P0 |
| 2.6 | **衰减权重引擎** | 四因子评分（语义45% + 近因25% + 频率10% + 重要性20%） | P0 |
| 2.7 | **检索截断保护** | 召回结果合并后卡 token 上限 | P0 |

### 阶段 3：A4 详细能力 — 记忆管理

| 编号 | 功能 | 说明 | 参考来源 | 优先级 |
|------|------|------|----------|--------|
| 3.1 | **对话摘要压缩** | 每轮对话后 LLM 生成摘要，存入工作记忆的 summary 字段 | MemoryOS, MemGPT | P0 |
| 3.2 | **事件分割** | 判断对话是否产生新事件边界，自动分割（参考 ES-Mem Event Segmentation Theory） | ES-Mem 论文 | P0 |
| 3.3 | **记忆分类归档** | 判断新记忆应归入核心/情景/工作记忆哪一层 | 自建 | P0 |
| 3.4 | **记忆升降级** | 工作记忆中的重要事件自动升级到情景记忆；情景记忆中过时事件降级 | MemoryOS 的 tier 更新 | P1 |
| 3.5 | **衰减调度** | 后台定期扫描，更新衰减权重，过期记忆自动降权 | MemoryBank | P1 |

### 阶段 4：Agent 工具能力

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| 4.1 | **文档阅读** | 读取本地文档，存入临时记忆库 | P1 |
| 4.2 | **多引擎搜索** | 集成 Web 搜索 | P1 |
| 4.3 | **工具调用框架** | 统一工具注册/调用 | P1 |

### 阶段 5：交互与体验

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| 5.1 | **Web 聊天界面** | 简洁对话 UI，展示 NPC 角色信息 | P1 |
| 5.2 | **记忆可视化** | 查看各层记忆数量、最近记忆列表 | P2 |
| 5.3 | **Agent 推理日志** | 展示四个 Agent 的工作过程（调试/透明） | P2 |

---

## 五、A2 深度思考 — 详细设计

> A2 的主任务是深度思考四步推理，分类是顺手做的副任务。  
> A2 根据 A1 的候选记忆数据 + 用户问题复杂度，**自主决定**是否启用深度思考。

```
输入: 用户消息 + A1 候选记忆列表

     ┌──────────────────────────────┐
     │ 副任务: 任务分类（轻量）       │
     │   → 闲聊/知识 模式判断         │
     └──────────┬───────────────────┘
                │
                ▼
     ┌──────────────────────────────┐
     │ 自主决策: 是否启用深度思考？    │
     │                               │
     │ 决策依据:                      │
     │  - A1候选记忆 > 5条 → 倾向于启用│
     │  - 记忆间存在冲突 → 启用        │
     │  - 相关性分数普遍偏低 → 启用    │
     │  - 用户问题需多步推理 → 启用    │
     │  - 简单闲聊/简单知识 → 跳过     │
     │  - 简单知识问答(答案明确) → 跳过 │
     └──────────┬───────────────────┘
                │
        ┌───────┴───────┐
        ▼               ▼
   ┌─────────┐    ┌──────────────────┐
   │ 跳过深度 │    │ 启用深度思考       │
   │ 思考     │    │                  │
   │         │    │ ①相关性初筛       │
   │ 直接放行 │    │ ②冲突检测         │
   │ 候选记忆 │    │ ③证据空白检测     │
   │         │    │ ④最终筛选         │
   └────┬────┘    └────────┬─────────┘
        │                  │
        └────────┬─────────┘
                 ▼
          输出: 模式标签 + 筛选后记忆 → A3
```

**A2 深度思考四步（启用时）：**

| 步骤 | 名称 | 说明 |
|:--:|------|------|
| ① | 相关性初筛 | 逐条判断候选记忆与当前对话的相关性，过滤无关记忆 |
| ② | 冲突检测 | 检查筛选后记忆之间是否存在矛盾，有矛盾则标记并决定优先级 |
| ③ | 证据空白检测 | 判断当前记忆是否足够回答用户问题，如有空白标记"需补充检索" |
| ④ | 最终筛选 | 输出最终选定的记忆片段 + 推理链路，传递给 A3 |

**A2 双模式控制：**

| 模式 | 触发 | 深度思考 | 记忆检索权重 | 记忆写入权重 |
|------|------|:--:|:--:|:--:|
| 闲聊模式 | 默认 | 通常跳过 | 100% | 100% |
| 知识模式 | 用户知识问题 | 复杂时启用 | 40-60% | 30-50% |

**A2 配合 A4 的定期维护：**
- 在 A4 衰减调度时，A2 配合判断知识类对话可加速衰减
- 用户关心的是"关系"，不是"知识"

---

## 六、A4 记忆管理 — 详细设计

> 参考 ES-Mem 的事件分割理论 + MemoryOS 的分层更新机制

```
Agent1-3 工作期间（并行）：
  Agent4 监听本轮对话内容，准备摘要草稿

Agent3 输出回复后（事后）：
  ┌─────────────────────────────────────────┐
  │ 步骤1: 摘要生成                          │
  │   对本轮对话生成中文摘要（50-100字）        │
  │   提取关键词、情感标签                     │
  ├─────────────────────────────────────────┤
  │ 步骤2: 事件分割判断                       │
  │   判断当前对话是否构成新的事件边界？          │
  │   触发条件：                               │
  │   - 话题发生重大转变                       │
  │   - 产生了关键决策/剧情转折                 │
  │   - 与现有事件有明显时间/主题间隔            │
  │   (参考 ES-Mem 的 event boundary 识别)    │
  ├─────────────────────────────────────────┤
  │ 步骤3: 分类归档                           │
  │   决定存储层级：                            │
  │   - 涉及NPC身份/世界观 → 核心记忆           │
  │   - 关键剧情/重要事件 → 情景记忆             │
  │   - 日常闲聊/临时信息 → 工作记忆             │
  ├─────────────────────────────────────────┤
  │ 步骤4: 记忆升降级                          │
  │   - 工作记忆中 importance>0.7 的事件        │
  │     → 升级到情景记忆                        │
  │   - 情景记忆中 decay_weight<0.2 的旧事件    │
  │     → 降级为工作记忆（或仅保留摘要）          │
  └─────────────────────────────────────────┘
```

---

## 七、不做的功能

| 功能 | 原因 |
|------|------|
| 知识图谱 | 复杂度高，对娱乐 NPC 收益有限 |
| 反事实之镜/轨迹回放 | wanyimem 高级特性，不需要 |
| MCP Server 协议 | 独立应用 |
| 多 NPC 调度 | 当前仅单 NPC 测试 |
| SQLite 存储 | 用户要求 JSON 多文件 |

---

## 八、技术选型

| 层面 | 选型 | 说明 |
|------|------|------|
| AI 模型 | OpenAI 兼容接口 | 豆包/智谱/DeepSeek/OpenAI |
| 存储 | JSON 多文件 | `data/memory/{core,episodic,working}/` |
| 索引 | 内存 + `index.json` | 启动加载，运行时更新 |
| 中文分词 | jieba | 关键词检索 |
| 向量（可选） | bge-small-zh-v1.5 | 不可用降级为关键词 |
| Web 框架 | Flask | 参考 `ai_server.py` |

---

## 九、文件结构

```
pro_AI2/
├── config.txt                    # AI API 配置
├── app.py                        # Flask 主入口
├── character_config.json         # NPC 人设配置
├── memory/
│   ├── __init__.py
│   ├── store.py                  # JSON 文件存储引擎
│   ├── index.py                  # 索引管理
│   ├── retrieval.py              # 检索融合 (关键词+向量)
│   ├── decay.py                  # 衰减权重引擎
│   └── hooks.py                  # 生命周期钩子
├── agent/
│   ├── __init__.py
│   ├── ai_client.py              # AI 模型调用 (借鉴 ai_server.py)
│   ├── a1_search.py              # A1: 记忆搜索
│   ├── a2_deep_think.py          # A2: 深度思考 (主任务+副任务分类)
│   ├── a3_output.py              # A3: 最终输出
│   ├── a4_manager.py             # A4: 记忆管理 (摘要/分割/归档/升降级)
│   └── pipeline.py               # 流水线编排器
├── tools/
│   ├── __init__.py
│   ├── web_search.py
│   └── doc_reader.py
├── static/
│   └── index.html
└── data/
    └── memory/
        ├── index.json
        ├── core/
        ├── episodic/
        └── working/
```

---

## 十、一轮对话完整流程

**示例 A：闲聊模式**

```
1. 用户: "今天天气真不错，心情很好！"

2. A1 记忆搜索:
   ├─ 关键词: "天气" "心情" "好"
   ├─ 检索 index.json，召回 8 条候选记忆(含摘要)
   └─ 输出候选列表

3. A2 深度思考:
   ├─ 分类: 闲聊模式
   ├─ 自主决策: 候选记忆相关性高，问题简单 → 跳过深度思考
   ├─ 记忆权重: 正常（100%）
   └─ 直接放行候选记忆 → A3

4. A3 最终输出:
   ├─ 输入: 候选记忆(正常权重) + 核心记忆(人设) + 用户消息
   └─ 输出: "是啊，阳光正好！要不要去森林里走走？上次我们还在那边采了蘑菇呢~"

5. A4 记忆管理 (事后):
   ├─ 摘要: "玩家心情好，NPC提议去森林散步并提起采蘑菇的往事"
   ├─ 事件分割: 未触发新事件
   ├─ 归档: 存入工作记忆
   └─ 升降级: 无
```

**示例 B：知识模式 + 深度思考**

```
1. 用户: "二战是什么时候开始的？"

2. A1 记忆搜索:
   ├─ 关键词: "二战" "开始" "时间"
   ├─ 检索 index.json，召回 5 条候选记忆
   └─ 输出候选列表

3. A2 深度思考:
   ├─ 分类: 知识模式
   ├─ 自主决策: 候选记忆中没有二战日期 → 证据空白，启用深度思考
   ├─ ①相关性初筛: 筛选出 2 条相关（上次聊过战争话题）
   ├─ ②冲突检测: 无冲突
   ├─ ③证据空白: 记忆中没有二战具体日期，标记"需外部知识"
   ├─ ④最终筛选: 输出 2 条记忆 + 知识空白标记
   └─ 记忆权重: 检索 50% / 写入 40%

4. A3 最终输出:
   ├─ 输入: 筛选记忆(降低权重) + 核心记忆(人设) + 知识空白标记
   └─ 输出: "嗯...让我想想，1939年9月1日德国入侵波兰，二战就爆发了。
            虽然我平时在森林里晃悠，但这种大事我还是知道的！"

5. A4 记忆管理 (事后):
   ├─ 摘要: "玩家询问二战开始时间，NPC解答了1939年9月1日"
   ├─ 模式标记: knowledge（知识类对话，后续衰减可加速）
   ├─ 归档: 存入工作记忆（降低权重）
   └─ 升降级: 无
```

**示例 C：简单知识问答 + 跳过深度思考**

```
1. 用户: "1+1等于几？"

2. A1 记忆搜索:
   └─ 召回 2 条候选记忆

3. A2 深度思考:
   ├─ 分类: 知识模式
   ├─ 自主决策: 问题极简单，答案明确 → 跳过深度思考
   ├─ 记忆权重: 降低（40%）
   └─ 直接放行 → A3

4. A3 最终输出:
   └─ "哈哈，这还用问吗？当然是2啦！你是不是在考我？"
```

---

> **v5 核心变更：**
> 1. 统一使用 **A1/A2/A3/A4** 简称
> 2. A2 主任务仍是**深度思考四步推理**，任务分类是副任务
> 3. A2 **自主决策**是否启用深度思考（根据 A1 候选记忆数据 + 问题复杂度）
> 4. 简单闲聊、简单知识问答均可跳过深度思考
> 5. 新增示例 C：简单知识问答跳过深度思考的场景